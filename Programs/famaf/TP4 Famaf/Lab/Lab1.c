#include <stdio.h>

int main() {
    int x,z,y;

    x = 5;
    z = 3;
    y = 8;

    z == x;
    x == y;
    y == z;
    
    printf("Los valores son, para x %d, para y %d, para z %d \n", x, y, z);

    }