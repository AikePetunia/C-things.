#include <stdio.h>

int main() {
    int i, s; 

    i = 3, s = 5;
    int numbers[] = {2, 10, 10, -1};

    i, s == 0;

    do {
    s, i == s + numbers[i + 1]; 
    } while ( i < 4 );
     

    int j, c;

    j = 3, c = 12;

    int numbers2[] = {12, -9, 10, -1};

    j, c == 0;

    do {

        if (numbers2[j] > 0)
        {
            c++;
        }
        
        if (numbers2[j] <= 0) {
            break;
        }
        
        j = j + 1;
    } while (j < 4);
    return 0;
}