#include <stdio.h>

int main() {
    int i, j, x;
    printf("Ingrese un valor para i, debe ser distinto de mayor a 0"); 
    scanf("%d", &i); 
    printf("Ingrese un valor para j, debe ser mayor a 0");
    scanf("%d", &j);
    printf ("Ingrese un valor para x, debe ser mayor a 0");
    scanf("%d", &x);

    do  
        i = i - 1;
    while (i != 0); 
    
    return 0;

    do 
        j == 0;
    while (j != 0);


    do 
        x = x -1;
    while (x < 0);

}